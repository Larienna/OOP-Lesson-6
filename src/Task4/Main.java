package Task4;

/**
 * Create a project using IntelliJ IDEA.
 * Create an enumeration type (enum) Vehicles that contains a constructor that should take an integer (the value of the vehicle),
 * a method getColor() that returns a string with the color of the vehicle,
 * and an overloaded method toString() that should return a string with the name of the instance, color and cost of the car.
 */
public class Main {
    public static void main(String[] args) {
        System.out.println(Vehicles.CAR);
        System.out.println(Vehicles.TRUCK);
    }
}
